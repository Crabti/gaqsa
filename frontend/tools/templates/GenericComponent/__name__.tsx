import React from 'react';
import { Typography } from 'antd';
import { Props } from './__name__.type';

const __name__: React.FC<Props> = ({}) => (
  <Typography>
    ...
  </Typography>
);

export default __name__;
