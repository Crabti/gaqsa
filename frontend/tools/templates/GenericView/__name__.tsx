import React from 'react';
import Title from 'components/Title';

const __name__: React.ViewComponent = ({ verboseName }) => (
  <Title viewName={verboseName} parentName="Gaqsa" />

);

export default __name__;
